import { Nullable } from "types";

export interface IModalForAddingResponsiblePerson {
  pspId: Nullable<string>;
  visible: boolean;
}

export interface IModalInformationOnOsuInfo {
  pspId: Nullable<string>;
  visible: boolean;
}
